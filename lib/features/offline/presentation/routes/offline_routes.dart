/// Offline feature route names
class OfflineRoutes {
  /// Route for sync settings screen
  static const syncSettings = '/settings/sync';

  // Private constructor to prevent instantiation
  OfflineRoutes._();
}
